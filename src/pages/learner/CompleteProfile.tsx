import SelfSignupCompleteProfile from "./SelfSignupCompleteProfile";

/** Unified onboarding for all learners (self-signup only). */
export default function CompleteProfile() {
  return <SelfSignupCompleteProfile />;
}
